{
  "format": 1,
  "name": "httpbin",
  "variables": {
    "apiToken": {
      "secret": true,
      "description": "Bearer token sent by the 'Auth Bearer' request. Add the real value to the gitignored sibling file environments/httpbin.secrets.json, e.g. { \"apiToken\": \"secret123\" } (httpbin.org/bearer accepts any non-empty token)."
    },
    "baseUrl": {
      "value": "https://httpbin.org"
    }
  }
}
